import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ["i.postimg.cc", "i.ibb.co", "i.ibb.co.com", "itbook.store", "lh3.googleusercontent.com", "drive.google.com", "images.unsplash.com"]
  },
};

export default nextConfig;
